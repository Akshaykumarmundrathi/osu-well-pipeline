# aws/.env.account2.template
# -----------------------------------------------------------
# Template only — the real file is generated by setup_account2.py.
# Run:  python aws/setup_account2.py --profile mano
# Then load before running orchestrate_robust.py or monitor.py.
#
# Windows:
#   for /f "tokens=1,2 delims==" %a in (aws\.env.account2) do set %a=%b
# bash / WSL:
#   set -a && source aws/.env.account2 && set +a
# -----------------------------------------------------------

# Account identifiers
ACCOUNT1_ID=225989338968
ACCOUNT2_ID=266087050585
AWS_DEFAULT_REGION=us-east-1

# S3
INPUT_BUCKET=osu-well-records-225989338968
OUTPUT_BUCKET=osu-pipeline-results-mano
INDEX_KEY=index/dataset_index.csv

# ECR image (lives in Account 1)
ECR_IMAGE=225989338968.dkr.ecr.us-east-1.amazonaws.com/osu-pipeline:v15-skip-anchor

# Batch
JOB_QUEUE=osu-pipeline-queue
JOB_DEF_NAME=osu-pipeline-job
LOG_GROUP=/aws/batch/osu-pipeline

# IAM roles (Account 2)
EXEC_ROLE_ARN=arn:aws:iam::266087050585:role/osu-batch-execution-role
TASK_ROLE_ARN=arn:aws:iam::266087050585:role/osu-batch-task-role

# Gemini model
GEMINI_FLASH_MODEL=gemini-2.0-flash-lite
