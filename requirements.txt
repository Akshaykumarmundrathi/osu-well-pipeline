# Core AWS / storage
boto3>=1.35
psycopg2-binary>=2.9

# Google APIs — pin protobuf + grpcio to avoid vision/generativeai conflict
protobuf==5.29.4
grpcio==1.67.1
googleapis-common-protos==1.65.0
google-cloud-vision==3.7.4
google-generativeai==0.8.3

# PDF / image processing
pymupdf>=1.24
opencv-python-headless>=4.10
pillow>=10.4

# ML / data
numpy>=1.26
pandas>=2.0
rapidfuzz>=3.10
matplotlib>=3.7
scipy>=1.11
pytesseract>=0.3.10

# torch installed separately in Dockerfile (CPU-only index URL)
